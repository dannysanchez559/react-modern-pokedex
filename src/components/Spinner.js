import React from "react";

const Spinner = () => {
  return (
    <img
      src="ballSpin.png"
      alt="spinning pokeball loader"
      className="ball-spin"
    />
  );
};

export default Spinner;
